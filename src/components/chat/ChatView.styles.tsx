import { List, Grid } from '@mui/material';
import styled from 'styled-components';

export const MessageList = styled(List)`
    height: 70vh;
    overflow-y: auto;
`;

export const ChatSectionGrid = styled(Grid)`
    width: 100%;
    height: 80vh;
`;
