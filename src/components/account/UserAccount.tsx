import { Fragment } from 'react';

const UserAccount = () => {
    return (
        <Fragment>
            <div>This is the user profile</div>
        </Fragment>
    );
};

export default UserAccount;
